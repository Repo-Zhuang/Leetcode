cin >> n;
    // cin.ignore();
    // vector<vector<int>> delayMatrix(n, vector<int>(n));

    // for (int i = 0; i < n ; i++) {

    //     string data;
    //     getline(cin, data);

    //     istringstream stream(data);
    //     for (int j = 0; j < n; j++) {
    //         stream >> delayMatrix[i][j];
    //     }
    // }